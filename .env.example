# 本地环境变量示例。复制为 .env 并填入真实值。
#
# .env 已在 .gitignore 里，**不会被提交**。永远不要把密钥写进 configs/ 或代码。
#
# 本项目只需要两类密钥：
#   1) 嵌入模型 —— DeepSeek **不提供** embeddings 接口（已实测 404），
#      所以嵌入用本地 Ollama（零成本、可离线、可复现，最适合教学）。
#   2) 对话模型 —— 用于生成与 LLM-as-judge。

# ---- 嵌入：本地 Ollama ----
# 拉模型：ollama pull bge-m3
EMBED_BASE_URL=http://127.0.0.1:11434/v1
EMBED_MODEL=bge-m3
EMBED_API_KEY=ollama

# ---- 对话：DeepSeek（OpenAI 兼容）----
# 可用模型：deepseek-flash / deepseek-v4-pro
CHAT_BASE_URL=https://api.deepseek.com/v1
CHAT_MODEL=deepseek-flash
CHAT_API_KEY=

# ---- 可选：GitHub token，用于提高抓取速率（匿名只有 60 次/小时）----
# GITHUB_TOKEN=

# ---- 可选：本地代理（国内直连 GitHub 大文件容易断流）----
# MAIXRAG_PROXY=http://127.0.0.1:7892
